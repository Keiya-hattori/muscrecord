# このリポジトリのルートに package.json がある想定（muscrecord フォルダ＝リポジトリ根）
name: Deploy GitHub Pages

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

# actions: write がないと upload-pages-artifact が失敗する（他スコープを省略すると既定が無効になるため）
permissions:
  contents: read
  actions: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Static export
        env:
          GITHUB_PAGES: "true"
          BASE_PATH: /${{ github.event.repository.name }}
          NEXT_PUBLIC_GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # 日付ページ大量生成でメモリ不足になるのを防ぐ
          NODE_OPTIONS: --max-old-space-size=8192
        run: npm run build

      - name: Disable Jekyll (allow _next etc.)
        run: touch out/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
